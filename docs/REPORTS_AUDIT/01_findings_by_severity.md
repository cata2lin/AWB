# AWB Reports — Discrepancy Audit (raw findings)


## [CRITICAL/AWB_BUG] VAT (fara_tva) is computed but NEVER applied — all SKU contribution/margin numbers are VAT-inclusive, contradicting AWB's own spec and main P&L  (sku-profitability)
- confidence: HIGH | needs_decision: False
- AWB: endpoint.py line 164 computes `dynamic_vat_rate = 0.19 if ref_date < cutoff_date else (config.vat_rate or 0.21)`. Grep confirms this variable appears NOWHERE else in the file. Revenue (line 193/262/307), COGS (line 280), transport, packaging, payment_fee, gt_commission, frisbo_fee, marketing, and the final contribution (line 332) and margin_pct (line 333), avg_selling_price (line 336) and the entire per-store block (lines 340-356) are all computed on raw VAT-INCLUSIVE (cu_tva) RON values. No division by (1+vat_rate) anywhere.
- Correct: Per PNL_KNOWLEDGE.md ('TVA is deductible — the real cost is always the fara_tva value; all percentage calculations use fara_tva values') and AWB's own main P&L engine profitability.py (tva_split() lines 447-450; net profit = profit_gross/(1+vat) line 350), the contribution margin and all reported revenue/cost figures for SKU profitability must be net-of-VAT (fara_tva). Domestic revenue, COGS, transport, payment/gt/frisbo/packaging fees should be divided by (1+dynamic_vat_rate); per-SKU marketing (DB table, treated like Facebook/TikTok foreign ads in main P&L) should NOT be VAT-divided (no_tva_split). The SKU report must agree with the main P&L on the same orders.
- Evidence: endpoint.py:164 (dead var); Grep for dynamic_vat_rate/vat_rate/fara_tva/cu_tva returns ONLY line 164. Contrast profitability.py:349-350 (`profit_net = profit_gross / (1 + dynamic_vat_rate)`) and :447-455 (tva_split/no_tva_split) and :612-614 (margins on fara_tva). PNL_KNOWLEDGE.md lines 144-184. Materiality: at vat 0.19-0.21 every contribution number is overstated and margin% distorted; Scripturi delivered revenue base is 32.5M RON.
- Fix @ endpoint.py lines 184-336 (per-order revenue/cost computation) + lines 329-336 (response build): Apply VAT consistently with the main P&L. Add a net helper net = lambda v: v/(1+dynamic_vat_rate) if dynamic_vat_rate>0 else v. Divide revenue, line_cogs, alloc_transport, alloc_packaging, alloc_payment, alloc_gt, alloc_frisbo by (1+dynamic_vat_rate) before aggregating (or apply at response time). Do NOT VAT-divide per-SKU marketing (treat as no_tva_split, matching how Facebook/TikTok/Google are no_tva in main P&L). Then contribution = revenue_fara - (cogs_fara+transport_fara+fees_fara+marketing) and margin on fara_tva. Add a regression test asserting SKU-report total_revenue == main P&L sales_delivered.fara_tva for an overlapping store/date window.

## [HIGH/AWB_BUG] profitability.py drops 'refused' / 'unsuccessful_delivery' into cat='other', contradicting its own PNL spec and the other two AWB mappings (transport loss silently lost)  (status-classification)
- confidence: HIGH | needs_decision: False
- AWB: In analytics/profitability.py the 5-category mapping (lines 293-302) has NO branch for aggregated_status 'refused' or 'unsuccessful_delivery'. They fall through to cat='other'. In that branch: COGS is NOT zeroed (line 305 only handles returned/cancelled), profit_gross is forced to 0 (lines 345-346), and the published P&L is assembled exclusively from stats['delivered'] (lines 459-473) plus a returns_cancelled income line that only sums stats['returned']+stats['cancelled'] (line 461). Result: refused orders are excluded from income's Returnate/Refuzate line AND their real transport-cost loss is dropped from the P&L entirely. This is internally inconsistent with deliverability.py (which counts 'refused'/'unsuccessful_delivery' as a shipped sub-bucket, line 60/93) and with compute_final_outcome() (which returns REFUSED, a PROBLEM_OUTCOME, computations.py lines 19-20/30).
- Correct: Map 'refused' and 'unsuccessful_delivery' to the 'returned' category, matching (a) profitability's OWN spec PNL_KNOWLEDGE.md lines 513-514 which lists 'refused' under cat='returned', (b) the deliverability tab grouping, and (c) Scripturi, where the equivalent 'Refuzata' bucket is part of 'plecate'/shipped and incurs a transport loss (Scripturi profitability.py:2015, P&L credits revenue only on 'Livrata' at :1992). In the 'returned' branch the code already zeroes COGS (line 306) and books profit_gross = -shipping_cost (line 342), which is the correct economic treatment for a refused/returned parcel.
- Evidence: profitability.py lines 295-302 (returned list is ['back_to_sender','returned','returning_to_sender','incorrect_address','lost'] — refused/unsuccessful_delivery absent; everything else hits cat='other' on line 302). Spec PNL_KNOWLEDGE.md lines 513-514: "elif status in ['returned_to_sender','returned','refused']: cat='returned'". deliverability.py line 60 counts ['refused','unsuccessful_delivery'] as refused/shipped. computations.py lines 19-20 REFUSED_WORKFLOW={'refused','unsuccessful_delivery'}. Spec deliverability_calculation_reference.md §2a: refused=165, unsuccessful_delivery=354 live aggregated_status orders (519+, plus shipment-level matches). Scripturi DB: 'Refuzata' = 39,729 orders = 14.31% of 277,662 total — confirming refusal is a large, transport-cost-bearing bucket for this exact business.
- Fix @ C:/Users/Admin/Desktop/AWB Print/awb-print-manager/backend/app/api/analytics/profitability.py lines 295-296: Add 'refused' and 'unsuccessful_delivery' to the cat=='returned' status list at line 295 (e.g. ['back_to_sender','returned','returning_to_sender','incorrect_address','lost','refused','unsuccessful_delivery']). This zeroes their COGS and books the -shipping transport loss, aligning profitability with deliverability.py, compute_final_outcome(), and PNL_KNOWLEDGE.md. Add a regression assertion that the three mappings agree on these two statuses.

## [HIGH/AWB_BUG] P&L drops 'refused'/'unsuccessful_delivery' orders into 'other' (mis-classified, contradicts deliverability tab)  (profitability-aggregate)
- confidence: HIGH | needs_decision: False
- AWB: profitability.py lines 295-302: returned = [back_to_sender, returned, returning_to_sender, incorrect_address, lost]; refused and unsuccessful_delivery are NOT listed and fall into 'other' (line 301-302). 'other' orders keep full COGS (only returned+cancelled are zeroed, line 305) and are charged a transport cost (line 336) but their profit_gross is forced to 0 (line 346), so the transport loss of a refused parcel is neither booked (as it is for 'returned' at line 342) nor excluded. The comment at line 292 falsely claims 'ALIGNED with deliverability tab status groups'.
- Correct: deliverability.py treats refused/unsuccessful_delivery as a delivery FAILURE inside 'shipped' (lines 60, 93) — a parcel that physically left and came back, economically a transport loss just like 'returned'. The P&L should classify refused+unsuccessful_delivery as 'returned' (loss = transport, COGS=0) to match both the deliverability tab and Scripturi, whose 'Refuzata' bucket = returned/refused.
- Evidence: deliverability.py line 60 refused set = ['refused','unsuccessful_delivery']; profitability.py line 295-302 omits them. AWB prod data (deliverability_calculation_reference.md): refused=165 + unsuccessful_delivery=354 = 519 orders mis-bucketed. Scripturi 'Refuzata' = 39,729 orders (14.3% of 277,662) is a first-class loss category, proving this is a material, recurring class of orders.
- Fix @ C:/Users/Admin/Desktop/AWB Print/awb-print-manager/backend/app/api/analytics/profitability.py lines 295-302 (cat classification).: Add 'refused' and 'unsuccessful_delivery' to the returned branch: elif status in ['back_to_sender','returning_to_sender','incorrect_address','lost','refused','unsuccessful_delivery']: cat='returned'. Remove the non-existent 'returned' literal. Then refused parcels correctly get COGS=0 and profit_gross=-shipping_cost. Update the line-292 comment to actually match deliverability.py (which also separates 'refused' but counts it in shipped).

## [HIGH/AWB_BUG] Per-order returned-order loss double-counts COGS (and adds packaging/commissions) — aggregate counts shipping only  (profitability-perorder-multiperiod)
- confidence: HIGH | needs_decision: False
- AWB: profitability_orders.py never zeros order_sku_cost for returned/cancelled orders (there is no analog of profitability.py lines 305-306), and line 220 computes the returned loss as profit_gross = -(order_sku_cost + shipping_cost + packaging_cost + gt_commission + payment_fee + frisbo_fee). The aggregate engine zeros COGS for returned/cancelled (profitability.py:305-306) and sets the returned loss to -(shipping_cost) ONLY (profitability.py:342).
- Correct: Returned orders should lose ONLY shipping (products come back and can be resold), matching the aggregate engine and the spec (PNL_KNOWLEDGE.md Cost Rules table: returned -> COGS 0, Profit = -shipping). COGS, packaging, gt_commission, payment_fee, frisbo_fee should NOT be part of the returned loss. Scripturi independently confirms this: it books COGS only on Livrata (delivered) and charges only transport on Refuzata.
- Evidence: profitability_orders.py:177-184 (cat assignment, no COGS zeroing), :219-220 (returned loss includes order_sku_cost+packaging+gt_commission+payment_fee+frisbo_fee). Contrast profitability.py:305-306 (order_sku_cost=0 for returned/cancelled) and :342 (profit_gross = -(shipping_cost)). Scripturi api/profitability.py:1992-2001 (COGS only on Livrata) + :2014-2016 (transport on plecate incl. Refuzata). DB magnitude: Refuzata = 39,729 orders, 5.78M RON revenue, 1.08M RON COGS.
- Fix @ C:/Users/Admin/Desktop/AWB Print/awb-print-manager/backend/app/api/analytics/profitability_orders.py:175-224: Mirror the aggregate: after computing cat, set order_sku_cost = 0 when cat in ('returned','cancelled'); and change the returned branch to profit_gross = -shipping_cost (drop sku/packaging/gt/payment/frisbo from the loss). This makes the per-order list reconcile with the aggregate returned loss.

## [HIGH/AWB_BUG] Per-order endpoint re-introduces agency commission that the aggregate and spec removed  (profitability-perorder-multiperiod)
- confidence: HIGH | needs_decision: False
- AWB: profitability_orders.py adds agency_commission = revenue * config.agency_commission_pct/100 (default 2.5%) for delivered & in_transit orders (lines 199-201) and includes it in total_costs (line 215) and therefore in profit_gross (lines 218, 222).
- Correct: Agency commission was deliberately removed from the per-order calculation and is now handled as a monthly business_costs entry. The aggregate engine has NO agency commission in total_costs (profitability.py:336) and the spec states this explicitly (PNL_KNOWLEDGE.md:573 'Agency commission was removed from per-order calculation ... The config field still exists for backward compatibility but is not used in the P&L formula'). Including it per-order double-counts it against the monthly business cost and makes the per-order profit ~2.5% of revenue lower than the aggregate.
- Evidence: profitability_orders.py:199-201, :215, :260 (agency_commission in output). Aggregate profitability.py:335-336 (comment 'agency commission REMOVED' and absent from total_costs). Spec PNL_KNOWLEDGE.md:336 and :573.
- Fix @ C:/Users/Admin/Desktop/AWB Print/awb-print-manager/backend/app/api/analytics/profitability_orders.py:198-201,215: Remove agency_commission from total_costs and from profit_gross (set to 0 / drop the term), matching the aggregate. Keep it as a display-only field at most, but never inside total_costs/profit. Then the per-order delivered profit equals the aggregate's per-order contribution.

## [HIGH/AWB_BUG] in_transit orders are counted as REALIZED delivered revenue + full COGS in per-SKU contribution  (sku-profitability)
- confidence: HIGH | needs_decision: True
- AWB: Line 304 comment 'delivered or in_transit — full calc': the else-branch (lines 303-320) is taken for BOTH cat=='delivered' and cat=='in_transit' (only 'returned' and 'cancelled' diverge; cancelled is continue'd at line 210). So an order still in transit contributes line_revenue, full line_cogs, transport and all fees to units_sold/revenue/cogs/contribution exactly as a delivered order, and increments orders_delivered (line 306).
- Correct: AWB's main P&L (PNL_KNOWLEDGE.md status table lines 522-531, profitability.py:434-441) keeps in_transit as PENDING/expected and reports realized profit from delivered only. Per-SKU realized profitability should be based on delivered orders; in_transit should either be excluded or surfaced separately (pending), not blended into realized contribution. Otherwise a SKU's reported profit includes revenue from parcels that may still be refused/returned (Scripturi shows 39,729 Refuzata vs 213k delivered — a non-trivial share of today's in_transit will become returns).
- Evidence: endpoint.py:198-207 (cat assignment), 304 ('delivered or in_transit — full calc'), 305-313. Contrast profitability.py:339-344 where in_transit profit is tracked but reported as pending (lines 434-437) and net_revenue=delivered only (line 462).
- Fix @ endpoint.py lines 196-320: Decide explicitly: either (a) restrict realized per-SKU contribution to cat=='delivered' and add a separate pending/in_transit bucket, mirroring main P&L; or (b) if blending is intentional, document it and rename orders_delivered to orders_shipped. Recommended: match main P&L (delivered = realized).

## [HIGH/AWB_BUG] Status classification in SKU endpoint diverges from AWB's own main P&L mapping (mis-buckets customer_pickup, in_parcel_locker, returning_to_sender, incorrect_address, lost, refused, fulfilled, redirected, on_hold)  (sku-profitability)
- confidence: HIGH | needs_decision: False
- AWB: SKU endpoint (lines 198-207): delivered = only 'delivered'; returned = only ['back_to_sender','returned']; cancelled = ['cancelled','voided']; in_transit = ['in_transit','out_for_delivery','customer_pickup']; else other. So customer_pickup and in_parcel_locker (which main P&L counts as DELIVERED) are treated as in_transit/other; returning_to_sender, incorrect_address, lost, refused (which main P&L counts as RETURNED) fall into 'other' and are processed via the full-calc else-branch with full revenue+COGS+fees.
- Correct: Main P&L profitability.py:293-302 is the authoritative AWB mapping: delivered=['delivered','customer_pickup','in_parcel_locker']; returned=['back_to_sender','returned','returning_to_sender','incorrect_address','lost']; cancelled=['cancelled','voided']; in_transit=['in_transit','out_for_delivery','fulfilled','redirected','deferred_delivery','on_hold']. The SKU report must use the SAME enum-to-category mapping so per-SKU figures reconcile with the P&L. Note even the two AWB files disagree with each other AND with PNL_KNOWLEDGE.md:511-519 — the mapping should be centralized.
- Evidence: endpoint.py:198-207 vs profitability.py:293-302. Real Frisbo aggregated_status has ~17 values; customer_pickup/in_parcel_locker are genuine deliveries being dropped from delivered, and refused/lost/returning_to_sender deliveries getting full COGS+fees via the 'other' else-branch.
- Fix @ endpoint.py lines 196-207: Extract the status-to-category mapping into one shared helper (e.g. app/services/status_classification.py) imported by both profitability.py and sku_profitability/endpoint.py so they cannot drift. At minimum, replace lines 198-207 with the main P&L mapping verbatim.

## [HIGH/AWB_WEAKER] Single VAT rate applied to all orders regardless of country (per-country VAT not implemented)  (profitability-aggregate)
- confidence: HIGH | needs_decision: True
- AWB: profitability.py lines 66-78 compute one dynamic_vat_rate (0.19 if ref_date<2025-08-01 else config.vat_rate default 0.21) and apply it to every order via tva_split() (lines 447-451) and profit_net (lines 349-352). There is no country dimension. The Store model (store.py) and Order model (order.py) have no country field; only currency exists.
- Correct: VAT must be per-country: Scripturi production setting vat_rates={RO:0.21,CZ:0.21,PL:0.23,BG:0.20}, applied via country_map prefix->country (Scripturi api/profitability.py lines 1957-1958, 2030-2035). Romanian retail VAT differs from PL (23%), BG (20%), CZ (21%). AWB sells via foreign stores (bonhaus.cz/.pl/.bg, nocturna.bg) — confirmed in Scripturi DB: CZ orders use CZK, PL use PLN, BG/BONBG use mostly EUR.
- Evidence: Scripturi DB profit_settings.vat_rates={"RO":0.21,"CZ":0.21,"PL":0.23,"BG":0.2}. Currency-by-prefix query: CZ=26192 CZK, PL=11195 PLN, BONBG=8501 EUR, BG=2483 EUR. Quantified net-revenue (fara_tva) error of flat-0.21 vs per-country across full delivered history: PL +10,197 RON (should be 0.23), BG -5,411 RON (should be 0.20), CZ 0 (matches 0.21). Delivered foreign revenue ~RON: CZ 1.82M, PL 0.76M, BG 0.79M.
- Fix @ C:/Users/Admin/Desktop/AWB Print/awb-print-manager/backend/app/api/analytics/profitability.py lines 447-451 (tva_split) and 349-352 (profit_net); requires a country resolver keyed off store name TLD or order.currency.: Add a country map (derive from store name suffix .bg/.cz/.pl or from order.currency: CZK->CZ, PLN->PL, BGN/EUR-on-BG-stores->BG, RON->RO) and a per-country vat_rates config {RO,CZ:0.21, PL:0.23, BG:0.20}. Make tva_split take the order's country VAT, and aggregate revenue/COGS/transport fara_tva per-country rather than via a single global rate. Keep the 2025-08-01 RO boundary for RO only. Add country to Store model so it is explicit rather than inferred.

## [HIGH/AWB_WEAKER] Velocity is NOT first-sale-aware: AWB divides every SKU by the full window, under-counting new SKUs by up to 5x  (sales-velocity)
- confidence: HIGH | needs_decision: False
- AWB: endpoint.py divides units by a single global period_days for EVERY SKU: velocity = _safe_div(units, period_days) (line 733), gross_velocity = _safe_div(gross_units, period_days) (line 789), variant var_velocity (line 807), store velocity (line 922). There is no per-SKU first-sale clamp anywhere. A SKU that first sold on day 20 of a 30-day window is still divided by ~30, so its velocity (and thus days_left_of_stock) is computed as if it sold for the whole period.
- Correct: Clamp the divisor to each SKU's active span, like Scripturi: first_sale_by_sku via MIN(date) over all history (product_analytics.py line 846-849), then eff_from = max(from_date, first_sale_date) and v_days = (to - eff_from).days + 1 clamped to [1, days_in_range] (lines 951-961 variant, 1021-1032 product). Velocity = units / active_days.
- Evidence: Scripturi product_analytics.db, 30-day window 2026-04-27..2026-05-26: 458 of 1347 selling SKUs (34.0%) have their first-ever sale inside the window. Examples (qty, first_date, clamped vs full velocity, ratio): SKU 166 qty=174 first=2026-05-15 -> 14.5 vs 5.8 (2.5x); SKU 164 qty=136 -> 11.33 vs 4.53 (2.5x); SKU 1673561 qty=5 first=2026-05-21 -> 0.83 vs 0.17 (5.0x). AWB's full-window divisor under-counts these by 1.1x to 5x, over-stating days_left_of_stock (endpoint.py line 794) and causing under-ordering of fresh winners.
- Fix @ C:/Users/Admin/Desktop/AWB Print/awb-print-manager/backend/app/api/sales_velocity/endpoint.py lines 733, 789, 807, 922 (and prev_velocity 738): Track the earliest sale date per group_key during the order loop (or query it). For each product/variant compute eff_days = clamp((to_bucharest_date(dt_to) - max(from_date, first_sale_date)).days + 1, 1, period_days) and divide units by eff_days instead of period_days for velocity, gross_velocity, and variant velocity. Mirror Scripturi's eff_from = max(window_start, first_sale_date) logic.

## [HIGH/AWB_WEAKER] No test-order exclusion anywhere in AWB analytics — every report over-counts orders (incl. delivered revenue/COGS)  (cross-cutting)
- confidence: HIGH | needs_decision: False
- AWB: AWB never excludes test orders from any analytics report. Two compounding facts: (1) The Order model has NO `tags` column (app/models/order.py — fields end at line 71, no tags), and the Frisbo parser (app/services/frisbo/parser.py) never extracts order tags from the Frisbo payload, so the data to filter on does not even exist in AWB's DB. (2) The only test/tag config that exists — ProfitabilityConfig.agency_commission_excluded_tags (default e.g. ['test'], models/profitability_config.py:29-30) — is NEVER applied: profitability_orders.py:42 reads `excluded_tags` into a variable but the per-order loop only checks `excluded_stores` (line 200), never `excluded_tags`. The main P&L engine profitability.py:119-124 selects ALL orders with only store/date conditions. Result: test orders inflate gross sales, delivered revenue, delivered COGS, order counts, deliverability denominators, velocity, and SKU-risk across every report. multi_period_pnl.py reuses profitability.py output so the quarterly P&L inherits the same over-count.
- Correct: Scripturi excludes test orders from ALL reports before any computation: api/profitability.py:1830-1847 loads profit_exclusion_rules (seeded with rule_type='tag', value='test' at line 249-251), then drops every order whose `tags` contains an excluded tag (`_is_excluded`), BEFORE building deliverability or profitability. AWB should (a) ingest Frisbo order tags into a new `Order.tags` column (migration + parser change), and (b) filter `tags ILIKE '%test%'` (configurable via the existing excluded_tags config, promoted from agency-only to a global report filter) in every analytics query/loop.
- Evidence: Scripturi DB quantification (profitability.db, 277,662 orders): 6,628 orders are test-tagged = 2.387% of ALL orders. By category: Netrimisa 4124, Lipsa awb 2173, Anulata 157, Livrata(delivered) 133, Refuzata 40. The 133 DELIVERED test orders carry revenue 13,698.39 RON and COGS 3,243.89 RON that would wrongly land in AWB's realized P&L; the remaining ~6,495 inflate order counts and deliverability/velocity denominators. AWB code: order.py has no tags field; parser.py:36-255 never reads raw_order['tags']; profitability_orders.py:42 vs 200 (excluded_tags loaded but unused); profitability.py:119-124 (no test filter).
- Fix @ C:/Users/Admin/Desktop/AWB Print/awb-print-manager/backend/app/models/order.py (add `tags` column + migration); app/services/frisbo/parser.py:~243 (extract raw_order.get('tags')); app/api/analytics/profitability.py:104-124 (add test-tag exclusion to conditions); app/api/analytics/profitability_orders.py, deliverability.py, product_deliverability.py, sales_velocity/endpoint.py, sku_risk/endpoint.py (same filter): Add an `Order.tags` String column (idempotent ALTER TABLE migration), populate it in the Frisbo parser, then exclude orders matching the configured test tags (reuse/rename agency_commission_excluded_tags into a global report filter) in every analytics endpoint's query — mirroring Scripturi's `_is_excluded`. Until tags are ingested, no exclusion is possible, so the schema+parser change is a prerequisite.

## [HIGH/AWB_WEAKER] Single VAT rate applied to all orders regardless of country — no store→country mapping infrastructure (per-country VAT impossible)  (cross-cutting)
- confidence: HIGH | needs_decision: False
- AWB: AWB applies ONE VAT rate to every order regardless of destination country. profitability.py:78 computes `dynamic_vat_rate = 0.19 if ref_date < 2025-08-01 else config.vat_rate(0.21)` once for the whole request, and tva_split()/the net-profit divisor (lines 349-350, 447-451) use it for ALL stores including the Czech (CZ), Polish (PL) and Bulgarian (BG) stores. profitability_orders.py:229 does the same per order. There is NO store→country map: Store model (models/store.py) has no country field, and ProfitabilityConfig has a single scalar vat_rate (models/profitability_config.py:45), not a per-country dict. So AWB structurally cannot charge PL 23% / BG 20% / CZ 21% VAT — it is hardwired to the RO rate for everyone.
- Correct: Scripturi maps each store prefix to a country (country_map, profitability.py:262-268) and looks up a per-country VAT rate (vat_rates {RO:0.21, CZ:0.21, PL:0.23, BG:0.20}, line 270) per store in the report loop: `country = country_map.get(prefix,'RO'); vat_rate = vat_rates.get(country, 0.19)` (lines 1957-1958), then divides that store's revenue/COGS/transport by (1+that store's vat_rate) (lines 2030-2035). AWB should add a `country` field to Store (or a prefix→country config), add a per-country vat_rates JSON to ProfitabilityConfig, resolve VAT per store/order, and apply it in both profitability.py and profitability_orders.py (the date<2025-08-01 → 0.19 RO rule stays but only for RO).
- Evidence: Scripturi DB: of 277,662 orders, NON-RO stores are 17.4% — CZ 26,192 (9.43%), PL 11,195 (4.03%), BG 11,017 (3.97%). Delivered revenue (converted to RON): CZ 1.82M, PL 0.76M, BG 0.79M. Applying RO VAT (0.21) instead of the correct per-country rate distorts net revenue: PL overstated by ~+10,197 RON and BG understated by ~-5,411 RON lifetime (CZ unaffected since CZ=RO=0.21). Per-store this is ~1.6% (PL) / ~0.8% (BG) of net revenue, and the SAME wrong divisor flows into each store's COGS, transport, gross/operating/net margins. AWB code: store.py (no country), profitability_config.py:45 (scalar vat_rate), profitability.py:78 & 447-451.
- Fix @ C:/Users/Admin/Desktop/AWB Print/awb-print-manager/backend/app/models/store.py (add country); app/models/profitability_config.py:45 (add per-country vat_rates JSON); app/api/analytics/profitability.py:78,447-451,612-614 and app/api/analytics/profitability_orders.py:229 (resolve VAT per store/country): Add Store.country (or a prefix→country map config) + a vat_rates JSON on ProfitabilityConfig, then compute VAT per store: country = store.country; rate = vat_rates[country] (keep the pre-2025-08-01 → 0.19 override only for RO). Apply the resolved per-store rate in tva_split and the net-profit divisor in both profitability.py and profitability_orders.py. Build the infrastructure first (migration + config), since none exists today.

## [HIGH/NEEDS_DECISION] Deliverability report does not exclude test orders; severely distorts Total / Shipped / Expedition-Rate for stores with test traffic (MAG: 47.9% vs correct 86.4% expedition rate)  (deliverability)
- confidence: HIGH | needs_decision: True
- AWB: deliverability.py counts ALL orders in the date/store window with no test-order exclusion. The Order model has no tags column, and the existing agency_commission_excluded_tags config (profitability_config.py:30) is dead code — loaded at profitability_orders.py:42 but never applied. Scripturi excludes tag=test from every report (profitability.py:1830-1847).
- Correct: Test orders must be excluded from deliverability counts so Total, Expediate (shipped) and Rata Expeditie reflect real traffic. Scripturi removes them before any aggregation. AWB needs an equivalent exclusion, but the IDENTIFIER differs: Frisbo orders carry no Shopify tags, so AWB must exclude via a test store_uid, an order_number pattern, or a Frisbo-sourced tags field — the user must specify which, since it depends on how test orders appear in Frisbo data.
- Evidence: Scripturi DB: 6,628/277,662 orders are tag=test. Store MAG has 6,497/14,410 (45.1%) test orders. Including them: MAG Total 14,410 vs correct 7,913 (+82%); Expedition Rate 47.9% vs correct 86.4% (38.5pp error); Deliverability Rate 76.0% vs 76.1% (negligible). Global deliverability rate 83.242% incl vs 83.246% excl (test orders are ~95% Netrimisa/Lipsa/Anulata, outside the shipped denominator). AWB analytics dir grep: zero tag/test/exclusion filtering. profitability_orders.py:42 loads excluded_tags but only excluded_stores is used (line 200).
- Fix @ C:/Users/Admin/Desktop/AWB Print/awb-print-manager/backend/app/api/analytics/deliverability.py (add a WHERE condition excluding test orders, e.g. Order.store_uid != <test store> or Order.order_number NOT LIKE <test pattern>); requires deciding the test-order identifier with the user. Related dead config: app/models/profitability_config.py:30 + app/api/analytics/profitability_orders.py:42.: Decide with the user how test orders are identifiable in Frisbo data (dedicated test store_uid, order_number prefix, or add a tags column to the Order model populated from Frisbo). Then add the corresponding exclusion condition to deliverability.py's conditions list (and ideally all analytics endpoints for consistency). Without a tags source, a literal tag=test filter is impossible — hence user decision required.

## [MEDIUM/AWB_BUG] Per-order endpoint subtracts packaging from profit; aggregate P&L excludes packaging  (profitability-perorder-multiperiod)
- confidence: HIGH | needs_decision: False
- AWB: profitability_orders.py sets packaging_cost = config.packaging_cost_per_order (line 196, default 3.7 RON) and includes it in total_costs (line 215) and thus profit_gross for every non-cancelled order, and in the returned loss (line 220).
- Correct: The aggregate engine deliberately excludes packaging from the operational sum and from the displayed/derived P&L (profitability.py:472 'removed Packaging from sum'; operational.packaging forced to {cu:0,fara:0} at line 649). The spec confirms packaging 'is NOT currently subtracted in the main P&L display' (PNL_KNOWLEDGE.md:576, :494). To reconcile, the per-order view must treat packaging the same way the aggregate does.
- Evidence: profitability_orders.py:196, :215, :218-220, :259. Aggregate profitability.py:472-475 (packaging removed from total_operational), :649 (packaging output zeroed), :336 (packaging not in total_costs). Spec PNL_KNOWLEDGE.md:576.
- Fix @ C:/Users/Admin/Desktop/AWB Print/awb-print-manager/backend/app/api/analytics/profitability_orders.py:196,215,220: Exclude packaging_cost from total_costs and profit_gross (and from the returned loss) so per-order profit matches the aggregate. If packaging is wanted later, it must be added to BOTH endpoints consistently (NEEDS_DECISION at the product level), but as of today the aggregate is authoritative and per-order must match it.

## [MEDIUM/AWB_BUG] Per-order status->category mapping is narrower and inconsistent with the aggregate engine  (profitability-perorder-multiperiod)
- confidence: HIGH | needs_decision: False
- AWB: profitability_orders.py:175-184 maps delivered = ['delivered'] only; returned = ['back_to_sender','returned']; in_transit = ['in_transit','out_for_delivery','customer_pickup']; everything else -> other. Note customer_pickup is treated as in_transit here.
- Correct: The aggregate engine (profitability.py:293-302, explicitly 'ALIGNED with deliverability tab status groups') maps delivered = ['delivered','customer_pickup','in_parcel_locker']; returned = ['back_to_sender','returned','returning_to_sender','incorrect_address','lost']; cancelled = ['cancelled','voided']; in_transit = ['in_transit','out_for_delivery','fulfilled','redirected','deferred_delivery','on_hold']. The per-order endpoint must use the SAME category mapping so an order is never delivered/returned/in_transit in one view and 'other' in the other.
- Evidence: profitability_orders.py:175-184 (and the SQL status filter :62-69 uses the same narrow sets). Aggregate profitability.py:293-302. Direct conflict: customer_pickup is 'delivered' in the aggregate but 'in_transit' per-order; returning_to_sender/incorrect_address/lost are 'returned' in aggregate but 'other' per-order; fulfilled/redirected/deferred_delivery/on_hold are 'in_transit' in aggregate but 'other' per-order.
- Fix @ C:/Users/Admin/Desktop/AWB Print/awb-print-manager/backend/app/api/analytics/profitability_orders.py:62-69,175-184: Replace the per-order category mapping (and the SQL status-filter branches) with the exact lists from profitability.py:293-302, including cancelled/voided. Ideally extract the mapping into one shared helper imported by both modules to prevent future drift.

## [MEDIUM/AWB_BUG] Per-SKU marketing is summed over entire calendar months in range, ignoring intra-month date filtering — over-attributes marketing for partial-month windows  (sku-profitability)
- confidence: HIGH | needs_decision: False
- AWB: Orders are filtered by exact UTC date bounds (lines 53-61) but marketing is queried by MONTH string only (lines 64-74 truncate to YYYY-MM; lines 94-101 query month >= mkt_date_from AND month <= mkt_date_to; lines 105-109 sum the FULL monthly amount). For a window like 2026-03-15..2026-03-20, all of March's SKU marketing spend is subtracted against ~6 days of revenue, crushing contribution; for default days=30 spanning two partial months, both full months are counted.
- Correct: Marketing must be pro-rated to the selected window (amount x days_in_window_within_month / days_in_month) or the report must restrict to whole months, matching how P&L marketing is date-bounded. Scripturi's per-SKU marketing (analytics_product_costs.marketing, product_profit_calculator --marketing-cost) is per-unit x qty, intrinsically window-proportional — AWB's monthly lump is not.
- Evidence: endpoint.py:64-74 (month truncation), 94-101 (month-range query), 105-109 (full-amount sum), vs order filtering by exact dates lines 53-61.
- Fix @ endpoint.py lines 64-109: Pro-rate each SkuMarketingCost entry by the fraction of that month covered by [date_from,date_to], or document that marketing is whole-month and force month-aligned ranges. Mirror the main P&L marketing date logic (profitability.py:494-500).

## [MEDIUM/AWB_BUG] period_days off-by-one: divides by N-1 instead of N inclusive days; also drops the last day from trend/sparkline charts  (sales-velocity)
- confidence: HIGH | needs_decision: False
- AWB: period_days = max((dt_to - dt_from).days, 1) (endpoint.py line 116). dt_to is end-of-day 23:59:59 (date_str_to_utc_end), so (dt_to - dt_from).days truncates to N-1 for an N-day inclusive range. Verified: explicit 2026-05-01..2026-05-31 -> period_days=30 (should be 31); default days=30 mode -> period_days=29 (should be 30). The daily_series and trends loops use range(period_days) (lines 761, 809, 890), so the final calendar day of the selected range is never emitted, even though its units are included in the aggregated totals/query (dt_to is the full inclusive bound).
- Correct: Scripturi: days_in_range = (d2 - d1).days + 1 (product_analytics.py line 803) gives the correct inclusive count. Velocity divisor and chart loops should both span all N inclusive days so the last day renders and totals match the chart.
- Evidence: Computed: 2026-05-01..2026-05-31 AWB period_days=30 vs Scripturi 31 (diff 1); default days=30 -> period_days=29; days=7 -> 6; days=90 -> 89. ~N/(N-1) over-statement of all velocities and KPI avg_units_per_day (~3.4% for 30d). Sparkline last day 2026-05-31 confirmed absent from the rendered series while its sales are in the totals.
- Fix @ C:/Users/Admin/Desktop/AWB Print/awb-print-manager/backend/app/api/sales_velocity/endpoint.py line 116 (and loops at 761, 809, 890): Set period_days = (to_bucharest_date(dt_to) - to_bucharest_date(dt_from)).days + 1 (inclusive). Make the daily_series/trends/variant loops iterate over all inclusive days so the final selected day appears and chart matches totals. This interacts with the first-sale clamp fix; apply both.

## [MEDIUM/AWB_BUG] Per-product denominator counts raw line-items, not distinct product-group appearances per order  (product-deliverability)
- confidence: HIGH | needs_decision: False
- AWB: product_deliverability.py lines 300-325 iterate raw line_items and unconditionally do g['total_orders'] += 1 plus the status-bucket increment per line item, with no de-duplication of (order, group_key). When the same SKU appears on multiple line items of one order, OR two distinct sibling variant-SKUs in one order resolve to the same barcode group, that single order's outcome is counted 2+ times into the SAME product group — inflating both the numerator (delivered/returned/refused) and denominator for that group's rates and distorting the min_orders=5 filter. The field is also mislabeled 'total_orders' though it is a line-item appearance count.
- Correct: Count each order once per DISTINCT product-group it contains (dedupe (order, group_key)), exactly as Scripturi does: it builds skus from a variant-id-keyed dict (collapsing same-variant lines) and increments total once per distinct SKU (api/profitability.py lines 719-725, 1518-1535, 2244-2293). A multi-product order legitimately contributes to multiple product rows (intended ~2.15x order count), but a product must not be counted more than once per order within its own row.
- Evidence: AWB product_deliverability.py:310 'g["total_orders"] += 1' inside 'for item in items_raw' with no per-order group_key set. Scripturi api/profitability.py:2253-2264 iterates DISTINCT sku_list and does s['total'] += 1 per distinct SKU; its skus list is deduplicated by variant id (line 1535 '; '.join(all_skus) where all_skus comes from variant_qty dict at line 1519). Quantified on Scripturi DB: 595,782 distinct-sku appearances / 277,536 orders = 2.147x (intended), and only 2/277,536 orders repeat the same SKU twice — confirming Scripturi never double-counts a product within an order, while AWB's barcode-merge + raw-line iteration can.
- Fix @ C:/Users/Admin/Desktop/AWB Print/awb-print-manager/backend/app/api/analytics/product_deliverability.py lines 287-325: In the order loop, first resolve each line item to its group_key, collect the set of distinct group_keys (and a per-group representative qty if unit metrics need preserving) for THIS order, then increment total_orders / status bucket / by_store ONCE per distinct group_key the order touches. Keep total_units as the qty sum (units are genuinely per-line). Rename total_orders to order_appearances for clarity.

## [MEDIUM/AWB_BUG] Per-store breakdown delivery_rate uses an inconsistent (smaller) 'shipped' denominator that excludes in_transit/out_for_delivery  (product-deliverability)
- confidence: HIGH | needs_decision: False
- AWB: The group-level shipped (line 352) includes in_transit + out_for_delivery, but the by_store accumulator (ZERO lambda line 282) and the accumulation guard (lines 323-325) only track delivered/returned/refused/cancelled. So the per-store s_shipped (line 364) = delivered + returned + refused, excluding in_transit and out_for_delivery, making each product's per-store delivery_rate (line 373) systematically higher than and inconsistent with its overall delivery_rate.
- Correct: Per-store shipped must use the same definition as the group total and the store-level deliverability report: delivered + in_transit + out_for_delivery + returned + refused. Scripturi is internally consistent here (plecate = livrata + refuzata + in_curs includes in-transit everywhere, lines 2298-2309).
- Evidence: AWB product_deliverability.py:352 group shipped includes in_transit+out_for_delivery; line 364 per-store s_shipped omits them; by_store dict line 282 has no in_transit/out_for_delivery keys; accumulation line 324 only bumps {delivered,returned,refused,cancelled}. Contrast deliverability.py:93 shipped = delivered + in_transit + out_for_delivery + returned + refused (store-level includes them). Scripturi api/profitability.py:2307 ps_plecate = livrata + refuzata + in_curs.
- Fix @ C:/Users/Admin/Desktop/AWB Print/awb-print-manager/backend/app/api/analytics/product_deliverability.py lines 282, 322-325, 363-374: Add in_transit and out_for_delivery to the by_store ZERO lambda and accumulate them (extend the guard at line 324 to include those buckets), then set s_shipped = delivered + in_transit + out_for_delivery + returned + refused so the per-store denominator matches the group/store-level definition.

## [MEDIUM/AWB_BUG] Unconvertible foreign currency silently kept as raw foreign number and aggregated into RON totals (treats foreign as RON)  (cross-cutting)
- confidence: HIGH | needs_decision: True
- AWB: When a non-RON order has no BNR rate within the 30-day lookback, profitability.py:223-227 sets `revenue = revenue_orig` (the raw foreign amount) and merely adds the currency to `unconvertible_currencies`. That raw foreign value is then summed straight into the RON `stats[cat]['revenue']`, per-store gross_sales, COGS-derived figures, and the P&L totals as if it were RON. The same silent fallthrough exists in profitability_orders.py:123-125. So e.g. an unconverted EUR or BGN amount is added 1:1 to RON revenue, understating (EUR) or distorting totals, and the only signal is a flag in the response the P&L math ignores.
- Correct: An order whose currency cannot be converted must NOT be mixed into RON sums at face value. Options (NEEDS minor decision but bug is clear): skip the order from monetary aggregation (still count it / surface it), or fall back to a configured default rate, or use the BNR last-known rate without the 30-day cap. Scripturi avoids this class of bug by using fixed monthly per-currency rates with hardcoded sensible defaults (EUR 4.97, CZK 0.21, PLN 1.16, BGN 2.54; profitability.py:497-500), so a rate is ALWAYS present. AWB should at minimum exclude unconvertible amounts from RON totals rather than adding raw foreign numbers, and ideally apply a configured fallback rate.
- Evidence: profitability.py:223-227 (`revenue = revenue_orig ... unconvertible_currencies.add(order_currency)`) followed by unconditional `stats[cat]['revenue'] += revenue` at line 356 and `per_store[...]['gross_sales'] += revenue` at line 387. profitability_orders.py:123-128 mirrors it. The 30-day cap that triggers this is get_rate_from_cache loop `range(1,31)` (exchange_rates.py:216-219) and the preload buffer (line 175). BNR publishes RON-quoted rates for EUR/USD/HUF/BGN/PLN/CZK so normal cases convert; the silent-as-RON path bites on gaps (new currency, long sync outage, very old orders before backfill).
- Fix @ C:/Users/Admin/Desktop/AWB Print/awb-print-manager/backend/app/api/analytics/profitability.py:223-227 (and the subsequent aggregation at 356-411); app/api/analytics/profitability_orders.py:123-128: On a missing FX rate, do not aggregate the raw foreign amount as RON: either skip the order's monetary contribution (while still flagging/counting it) or apply a configured fallback rate. Add a regression test asserting an unconvertible EUR order does not add its face value to RON revenue.

## [MEDIUM/AWB_WEAKER] Three hand-maintained status mappings instead of one shared classifier (structural root cause of drift)  (status-classification)
- confidence: HIGH | needs_decision: True
- AWB: AWB maintains three independent status-classification tables that must be kept in lockstep manually: (1) deliverability.py SQL CASE (lines 52-61), (2) profitability.py 5-category if/elif (lines 293-302), (3) compute_final_outcome() constant sets (computations.py lines 14-27). There is no single canonical aggregated_status->category map; each call site re-encodes it. This is exactly how the 'refused' inconsistency (finding 1) arose — the value was added to two of the three places and missed in the third.
- Correct: Mirror Scripturi's single source of truth. Scripturi has ONE classifier (_map_status, scripturi profitability.py:341) feeding deliverability, P&L, and per-SKU identically. AWB should extract a shared module (e.g. app/core/status_classification.py) exposing the canonical aggregated_status->category dict (+ the shipment/fulfillment fallback layering for compute_final_outcome). deliverability would build its SQL CASE from it, profitability would derive 'cat' from it, and compute_final_outcome would layer fallbacks on top — guaranteeing the three can never disagree again.
- Evidence: Three separate definitions: deliverability.py:52-61; profitability.py:293-302; computations.py:14-27. multi_period_pnl.py confirmed to add no 4th mapping (it reuses profitability's status_breakdown via _build_period_data, multi_period_pnl.py:80-101). Scripturi single classifier: api/profitability.py:341 _map_status, consumed at :1384, :1560, :1775, :2743, :2906 etc.
- Fix @ C:/Users/Admin/Desktop/AWB Print/awb-print-manager/backend/app/api/analytics/ (deliverability.py, profitability.py) + backend/app/api/sku_risk/computations.py — new shared module: Create one canonical aggregated_status->category map in a shared module; refactor deliverability SQL CASE, profitability cat-selection, and compute_final_outcome to consume it. Unifies on a single source of truth like Scripturi and prevents future drift. Fold finding 1's fix into this shared map.

## [MEDIUM/AWB_WEAKER] No test-order exclusion (test orders inflate gross sales, delivered counts, COGS and profit)  (profitability-aggregate)
- confidence: MEDIUM | needs_decision: True
- AWB: profitability.py only excludes specific SKUs from COGS (Product.exclude_from_stock, lines 130-138). There is no order-level test exclusion; the Order model has no tags column.
- Correct: Scripturi excludes orders whose tags contain 'test' (exclude_test=true; profit_exclusion_rules tag rule; api/profitability.py lines 1830-1847). Test/internal orders should not appear in revenue, COGS, delivered counts, or profit.
- Evidence: grep of profitability.py shows only SKU-level exclusion (lines 130-138), no tag/test filter. Scripturi profit_exclusion_rules seeds ('tag','test') and _is_excluded() drops them before aggregation.
- Fix @ C:/Users/Admin/Desktop/AWB Print/awb-print-manager/backend/app/api/analytics/profitability.py order query at lines 118-124 (and order.py model).: Add a tags field to the Order model (migration: ADD COLUMN tags) populated from Frisbo/Shopify, then exclude orders whose tags contain configurable test markers before aggregation. Interim heuristic: exclude by customer_name/email patterns. Without a tags column this cannot be done robustly today.

## [MEDIUM/AWB_WEAKER] Single global VAT rate vs per-country VAT (PL 23%, BG 20% differ from RO 21%)  (sku-profitability)
- confidence: HIGH | needs_decision: True
- AWB: Even once VAT is applied (finding 1), the endpoint would use one dynamic_vat_rate for every order regardless of destination country (line 164).
- Correct: Scripturi profit_settings.vat_rates = {RO:0.21, CZ:0.21, PL:0.23, BG:0.20} with a country_map from store prefix to country, applying the correct VAT per order. For SKUs sold across stores in different countries the net revenue divisor should vary by country.
- Evidence: Scripturi DB: delivered revenue by country RO 70.8% @0.21, CZ 26.7% @0.21 (coincides with RO post-2025-08), PL 2.0% @0.23, BG 0.5% @0.20. So the per-country error is bounded to PL+BG ~2.5% of revenue (CZ luckily equals the RO rate), but it is a real systematic bias that grows if non-RO/CZ stores scale.
- Fix @ endpoint.py line 164 + per-order loop (needs store-to-country lookup, currently absent): Adopt per-country VAT: add a country/vat map to ProfitabilityConfig (or Store model) and select vat per order by store country, as Scripturi does. Lower priority than finding 1 given current CZ=RO coincidence, but design it in when VAT application is fixed.

## [MEDIUM/AWB_WEAKER] No test-order / exclusion-rule filtering in SKU profitability  (sku-profitability)
- confidence: MEDIUM | needs_decision: False
- AWB: The endpoint loads ALL orders matching store/date (lines 77-81) with no exclusion of test orders, test SKUs, or tag-based exclusions. Cancelled orders are skipped (line 210) but test/sample data is not.
- Correct: Scripturi applies exclude_test=true with an exclusion rule (tag='test') and profit_exclusion_rules (by sku/tag) before aggregation (profitability.py:1834-1845, 2515-2530). Test orders inflate units/revenue for affected SKUs.
- Evidence: endpoint.py:77-81 (no exclusion); Scripturi profit_settings.exclude_test + profit_exclusion_rules. AWB config has agency_commission_excluded_tags/stores fields (profitability_config.py:21-22) but they are unused here.
- Fix @ endpoint.py lines 77-81 + order loop: Apply an exclusion filter (test tag / configured excluded SKUs/tags) consistent with the main P&L and Scripturi before aggregating per-SKU. Reuse the existing config exclusion fields.

## [MEDIUM/NEEDS_DECISION] VAT period boundary uses one rate for the whole report instead of per-order date  (profitability-aggregate)
- confidence: HIGH | needs_decision: True
- AWB: profitability.py lines 66-78: ref_date = strptime(date_to) (or utcnow); dynamic_vat_rate chosen once for the entire result set based on whether ref_date < 2025-08-01. Every order in the period is split at that single rate.
- Correct: For a report spanning the 2025-08-01 RO VAT change, orders before the cutoff should use 0.19 and orders on/after should use config.vat_rate (0.21), keyed on each order.frisbo_created_at. Anchoring to date_to means a 2025-07-15..2025-08-15 report taxes the July orders at 0.21 (wrong) or, if anchored differently, the August orders at 0.19.
- Evidence: profitability.py line 70 ref_date=datetime.strptime(date_to,...); line 78 dynamic_vat_rate is a scalar. No per-order date check exists in the aggregation loop (lines 209-411). Scripturi is month-scoped so never spans the boundary.
- Fix @ C:/Users/Admin/Desktop/AWB Print/awb-print-manager/backend/app/api/analytics/profitability.py lines 66-78 and the per-order loop ~209-365.: Compute the VAT rate per order from order.frisbo_created_at vs the 2025-08-01 cutoff and combine with per-country rate (finding 1). Aggregate fara_tva amounts per order rather than dividing global totals by one rate. If the business only ever runs intra-month reports, document this as an accepted limitation instead.

## [MEDIUM/NEEDS_DECISION] SKUs with no cost record are counted with COGS=0, inflating their contribution while only flagged via has_cost=false  (sku-profitability)
- confidence: MEDIUM | needs_decision: True
- AWB: Lines 278-283: if sku_costs_map has no entry, line_cogs=0.0 and the sku is added to missing_cost_skus, but its full revenue and all fees still flow into contribution (lines 305-313). The product appears highly profitable purely because its cost is unknown; has_cost=false (line 378) is the only signal.
- Correct: Scripturi tracks cogs_missing per order (profit_orders.cogs_missing — 5,672 orders affected in its DB) and surfaces missing-COGS details explicitly (profitability.py product-stats / cogs_missing_details) rather than silently treating cost as zero. AWB should at minimum exclude missing-COGS SKUs from the aggregate margin headline, or clearly partition them, so a missing cost is not read as 100%-margin profit.
- Evidence: endpoint.py:278-283, 305-313, 378; summary returns missing_cost_skus list (line 400) but contribution/margin already include them at zero cost. Scripturi cogs_missing count = 5,672 orders.
- Fix @ endpoint.py lines 278-336: Business decision: keep zero-COGS-but-flagged (current), OR null contribution/margin for has_cost=false SKUs, OR exclude them from summary totals. Recommend null-ing contribution/margin when has_cost is false and excluding from avg_margin so the headline is not inflated.

## [MEDIUM/NEEDS_DECISION] Headline velocity uses NET (delivered-only) units while Scripturi uses GROSS (orders placed) — ~23% structural gap, different rankings  (sales-velocity)
- confidence: HIGH | needs_decision: True
- AWB: AWB's primary velocity, the products sort key (line 867), units_sold, KPI total_units/avg_units_per_day, alerts (hot/cold/new_star), and store_comparison all use delivered-only units (is_delivered branch, lines 588-591; velocity = units_sold/period_days, line 733). Gross is tracked separately (total_units, non-cancelled) and only used for min_units filtering and days_left effective_velocity (lines 789-792).
- Correct: Scripturi's velocity headline is GROSS: qty_sold counts every non-VOIDED/non-REFUNDED order line item (product_analytics.py line 536, 564), with no delivered/net concept. For replenishment, demand (orders placed) is the appropriate basis, not eventual deliveries. AWB already correctly uses gross for days_left but inconsistently uses net for the headline number, sort order, KPIs and alerts.
- Evidence: Scripturi profitability DB: delivered (Livrata) = 213408 / 277662 = 76.9% of gross; cancelled (Anulata) only 3.1%. So AWB's net headline velocity sits ~23% below the gross velocity Scripturi reports, and the products are sorted/ranked on net, which can reorder the table vs Scripturi and depress recent-window numbers (orders placed in the last few days not yet delivered).
- Fix @ C:/Users/Admin/Desktop/AWB Print/awb-print-manager/backend/app/api/sales_velocity/endpoint.py lines 733, 867 (sort), 870/880 (KPIs), 922 (store velocity), 932-987 (alerts): Business decision: either (a) switch the headline velocity/sort/KPIs/alerts to gross (gross_units/eff_days) to match Scripturi's orders-placed semantics for replenishment, keeping net as a secondary delivered-rate metric; or (b) explicitly document that AWB intentionally headlines delivered velocity. Recommend (a) for consistency with Scripturi and with AWB's own days_left logic.

## [LOW/AWB_BETTER] COGS=0 for returned/cancelled and COGS kept for 'other' — correct per spec  (profitability-aggregate)
- confidence: HIGH | needs_decision: False
- AWB: profitability.py lines 304-306 zero COGS only for returned+cancelled; in_transit and 'other' keep COGS. Displayed P&L COGS (line 465) = delivered-only sku_costs.
- Correct: Matches PNL_KNOWLEDGE.md line 530 ('other' keeps COGS) and lines 96-98 (displayed COGS = delivered only). Scripturi sums COGS only for Livrata — equivalent for the displayed line. No discrepancy.
- Evidence: profitability.py lines 304-306, 465; PNL_KNOWLEDGE.md lines 96-98, 524-530; Scripturi api/profitability.py lines 1992-2001 (COGS only when cat=='Livrata').
- Fix @ N/A — no fix needed.: No change. Confirmed correct and consistent with spec; document equivalence.

## [LOW/AWB_BETTER] Real per-order CSV transport (5-step fallback) and daily BNR FX are more accurate than Scripturi  (profitability-aggregate)
- confidence: HIGH | needs_decision: False
- AWB: profitability.py lines 236-258 use real per-order transport_cost from courier-CSV import with same-SKU/brand-avg/customer-paid fallbacks; FX via daily BNR rates per order date (lines 218-227).
- Correct: Scripturi uses a fixed monthly cost_per_parcel x plecate (api/profitability.py lines 2012-2016) and one averaged/hardcoded monthly FX (lines 433-511). AWB's per-order real costs and daily FX are strictly more precise. No fix.
- Evidence: profitability.py lines 236-258 (transport fallback chain) and 218-227 (per-date FX). Scripturi lines 2012-2016 (fixed transport) and 497-500 (hardcoded FX defaults EUR 4.97, BGN 2.54, PLN 1.16, CZK 0.21).
- Fix @ N/A — no fix needed.: No change. Document AWB as the more correct implementation for transport and FX.

## [LOW/AWB_BETTER] Multi-period CM3 (contribution margin) and operating_profit reconstruction are correct and reconcile with the aggregate  (profitability-perorder-multiperiod)
- confidence: HIGH | needs_decision: False
- AWB: multi_period_pnl.py defines operating_profit = gross_profit - (shipping+frisbo+payment+gt+warehouse) and CM3 = operating_profit - marketing (lines 86-95, 212-216, 300-305). The consolidated row is taken directly from the authoritative aggregate pnl dict (lines 258-307), so it reconciles by construction; subsidiary/individual rows are summed from pnl_by_store.
- Correct: This matches the aggregate's operating_profit (profitability.py:484-487) and the standard CM3 = contribution margin BEFORE fixed/overhead costs (fixed_costs intentionally excluded). The quarter percentages are recomputed on summed totals, not averaged (lines 350-358), which is correct. No fix needed.
- Evidence: multi_period_pnl.py:86-95 (_extract_single_store), :212-231 (_extract_subsidiary_data), :258-307 (consolidated from aggregate pnl), :333-358 (_build_quarter recomputes pct on totals). Matches profitability.py:472-487.
- Fix @ n/a (no fix; documents correctness). Minor: per-component pre-rounded fara_tva summation vs aggregate split-the-sum yields only RON-level rounding differences in subsidiary/individual rows.: No change required. Optionally, to remove the tiny rounding gap, sum raw (un-rounded) component values before the single round; not material.

## [LOW/AWB_BETTER] days_left_of_stock includes incoming PO stock and falls back to gross velocity — AWB is more correct than Scripturi  (sales-velocity)
- confidence: HIGH | needs_decision: False
- AWB: effective_stock = stock + po_incoming (line 778); days_left_of_stock = effective_stock / effective_velocity where effective_velocity = gross_velocity if >0 else net (lines 789-798). Incoming approved/partially-received PO quantities extend coverage, and gross velocity avoids the short-window net=0 trap.
- Correct: Scripturi computes days_remaining = int(inventory / velocity) using bare on-hand inventory and gross velocity only (product_analytics.py lines 965, 1036), ignoring incoming POs. AWB's inclusion of confirmed incoming stock is the more accurate coverage estimate.
- Evidence: endpoint.py lines 690-708 load PO incoming from PurchaseOrderItem (quantity - received_qty) for APPROVED/PARTIALLY_RECEIVED POs and map to group keys; line 778 adds it to stock. Scripturi has no equivalent.
- Fix @ n/a (AWB superior): No fix. Document that AWB intentionally factors confirmed incoming PO stock into days_left, which Scripturi does not.

## [LOW/AWB_BETTER] Per-store timezone bounds only applied in explicit-date mode; default days-mode ignores CZ/PL store timezones  (sales-velocity)
- confidence: MEDIUM | needs_decision: False
- AWB: _get_store_bounds applies the store-specific timezone (Europe/Prague, Europe/Warsaw) only when explicit_dates is true (endpoint.py line 129); in default days-mode it returns (dt_from, dt_to) = Bucharest bounds for ALL stores (lines 139-140). So a CZ/PL order near local midnight is classified into current vs previous period differently depending on whether the user picked explicit dates or used the days preset.
- Correct: Scripturi uses Europe/Bucharest uniformly for ALL stores in both modes (product_analytics.py lines 472-478), so AWB's per-store TZ is strictly more correct where it applies; the bug is only that it is not applied consistently across the two entry paths. Impact is limited to a handful of near-midnight CZ/PL orders.
- Evidence: endpoint.py line 129 condition `if tz_name and explicit_dates:` gates TZ-awareness on explicit_dates; STORE_TIMEZONE_MAP only contains bonhaus.cz/.pl (lines 51-59). BG stores intentionally share Bucharest offset. Scripturi has no per-store TZ at all.
- Fix @ C:/Users/Admin/Desktop/AWB Print/awb-print-manager/backend/app/api/sales_velocity/endpoint.py lines 124-142: Optional consistency fix: apply the store timezone in default days-mode too (compute per-store local-midnight bounds from the same days window), or document that TZ-awareness is explicit-date-only. Low priority; AWB is already ahead of Scripturi here.

## [LOW/AWB_BETTER] Barcode-group and Nubra isolation grouping is correct and consistent with Viteza Vanzari; stronger than Scripturi's grouping  (product-deliverability)
- confidence: HIGH | needs_decision: False
- AWB: Grouping (lines 155-268) uses the shared product_grouping.py (classify_stores, pick_best_primary), barcode-first then SKU fallback, with Nubra isolated via the '::nubra' group-key suffix (_group_key lines 264-268).
- Correct: This matches sales_velocity/endpoint.py exactly and is more capable than Scripturi, which keys purely by raw SKU and cannot merge sibling variants sharing a barcode. No fix to the grouping itself.
- Evidence: product_deliverability.py:155-268 mirrors sales_velocity/endpoint.py:231-401; both import from app/services/product_grouping.py; Nubra UID isolation identical (NUBRA_UID + ::nubra suffix). Scripturi api/profitability.py:2249 splits raw skus string only.
- Fix @ N/A: No change. Note this stronger grouping is what makes FINDING 1's intra-order double-count possible, so the FINDING 1 dedup fix should be applied on top of this grouping.

## [LOW/AWB_BETTER] Daily BNR FX with last-known-rate fallback is more accurate than Scripturi's fixed monthly-average / hardcoded-default rates  (cross-cutting)
- confidence: HIGH | needs_decision: False
- AWB: AWB converts each order with the official BNR RON rate for that order's Bucharest date, falling back to the most recent prior published rate within 30 days (exchange_rates.py get_rate/get_rate_from_cache), rates synced daily from bnr.ro. This reflects intra-month FX movement and uses the legally-relevant central-bank rate.
- Correct: AWB is more correct here — no change. Document that the only risk is the unconvertible-fallback bug filed separately; the daily-rate approach itself should be kept.
- Evidence: Scripturi uses a single month-average rate per currency (frankfurter.app ECB averaged over the month, profitability.py:464-486) and, on fetch failure, hardcoded constants EUR 4.97 / CZK 0.21 / PLN 1.16 / BGN 2.54 (lines 497-500) applied uniformly to every order in the month. AWB's per-day BNR rate is finer-grained and matches Romanian accounting practice (BNR daily reference rate). AWB: exchange_rates.py:25 (BNR feed), 118-144 (per-date lookup with prior-day fallback).
- Fix @ N/A (no fix — keep AWB's daily BNR approach): No change. Keep daily BNR conversion; only fix the unconvertible-fallback handling tracked in the separate AWB_BUG finding.

## [LOW/AWB_BETTER] compute_final_outcome() documented bug is ALREADY FIXED in current code (verified against spec's Corrected table)  (cross-cutting)
- confidence: HIGH | needs_decision: False
- AWB: The deliverability spec doc flags compute_final_outcome() as having a 'Current (buggy)' version that drops in_transit/out_for_delivery/customer_pickup/generated_awb/on_hold/ready_for_picking/deferred_delivery into OTHER. The ACTUAL current code in sku_risk/computations.py matches the spec's 'Corrected' table, not the buggy one — so this systemic ~3,362-order misclassification (feeding SKU Risk + Sales Velocity) is resolved.
- Correct: No fix — verified correct. IN_TRANSIT_WORKFLOW includes in_transit & out_for_delivery (line 23); IN_TRANSIT_SHIPMENT includes in_transit, out_for_delivery, customer_pickup (line 24); DELIVERY_PROBLEM_SHIPMENT includes deferred_delivery (line 22); NOT_SHIPPED_SHIPMENT includes generated_awb (line 26); NOT_SHIPPED_FULFILLMENT includes on_hold & ready_for_picking (line 27). All match the Corrected table.
- Evidence: C:/Users/Admin/Desktop/AWB Print/awb-print-manager/backend/app/api/sku_risk/computations.py:14-63 vs deliverability_calculation_reference.md section 5 'Corrected' table (lines 201-232). Every status the doc says to move out of OTHER is present in the correct set in the code.
- Fix @ N/A (already corrected): No action. Confirmed the previously-documented OTHER-misclassification bug is fixed in the current code; SKU Risk and Sales Velocity outcome mapping is correct.

## [LOW/AWB_BUG] orders_count/orders_delivered is incremented per matched line-item and includes in_transit — it is not an order count  (sku-profitability)
- confidence: MEDIUM | needs_decision: False
- AWB: Line 306 increments sd['orders_delivered'] += 1 inside the per-line-item loop (lines 274-320), so a SKU appearing across multiple lines over-counts; it also includes in_transit orders. The response field orders_count (line 364) is therefore neither distinct-order nor delivered-only.
- Correct: orders_count should be the number of DISTINCT delivered orders containing that SKU (Scripturi counts distinct orders via order_lines / status_category='Livrata'). Per-line increment double-counts when a SKU recurs and conflates in_transit.
- Evidence: endpoint.py:274 (loop over line_data), 306 (increment per line), exposed at 364. Scripturi product-stats counts per-order categories (profitability.py:2253-2293).
- Fix @ endpoint.py lines 304-306, 364: Track delivered order membership per SKU with a set of order ids (increment once per order, delivered-only) rather than per line item; rename/clarify the field.

## [LOW/DATA_SOURCE_DIFF] Deliverability SQL CASE buckets and rate formulas conform exactly to spec sections 3-4 (in_transit bucket complete; not buggy)  (deliverability)
- confidence: HIGH | needs_decision: False
- AWB: deliverability.py:52-61 SQL CASE and :93-152 formulas. in_transit bucket = in_transit, fulfilled, redirected, deferred_delivery, on_hold (in_transit IS present). shipped = delivered+in_transit+out_for_delivery+returned+refused. delivery/deliverability = delivered/shipped; cancelled_rate uses total denominator; expedition_rate = shipped/total.
- Correct: Matches deliverability_calculation_reference.md 'Corrected' mapping (section 3) and formula table (section 4) exactly. The documented 'in_transit falls to OTHER' bug applies only to compute_final_outcome() (Sales Velocity / SKU Risk), not to this report, and that function is also already fixed (computations.py:23-27 matches the Corrected table).
- Evidence: Spec section 3 In-Transit row = in_transit, fulfilled, redirected, deferred_delivery, on_hold — identical to deliverability.py:55. Spec section 4 formula table identical to deliverability.py:96-103. computations.py:23 IN_TRANSIT_WORKFLOW={fulfilled,in_transit,out_for_delivery}, :24 IN_TRANSIT_SHIPMENT includes customer_pickup, :26 includes generated_awb, :27 includes on_hold+ready_for_picking — all per the 'Corrected ✅' table.
- Fix @ n/a (no fix): No change. Confirmed spec-conformant; the documented buggy state has already been corrected in current code.

## [LOW/DATA_SOURCE_DIFF] Refused+Returned merged-vs-split and Netrimisa/Lipsa-awb absence are legitimate data-source differences with equivalent core logic  (deliverability)
- confidence: HIGH | needs_decision: False
- AWB: AWB keeps 'returned' (back_to_sender, returning_to_sender, incorrect_address, lost) and 'refused' (refused, unsuccessful_delivery) as two buckets but SUMS both into shipped (deliverability.py:93). It has no Lipsa-awb pseudo-state because Frisbo aggregated_status lacks a fulfilled-but-no-AWB value.
- Correct: Scripturi merges refused+returned into one 'Refuzata' category and its denominator plecate = Livrata + In-curs + Refuzata (profitability.py:1929, 2298), excluding Anulata/Netrimisa/Lipsa-awb. AWB's denominator (delivered + in_transit + ofd + returned + refused) is arithmetically identical, and AWB correctly excludes cancelled/processing/waiting/ready (= Scripturi's Anulata/Netrimisa). Equivalent.
- Evidence: Scripturi plecate denominator at profitability.py:2298 (s['livrata']+s['refuzata']+s['in_curs']) and 1929. AWB shipped at deliverability.py:93. Scripturi category counts: Livrata 213,408; Refuzata 39,729 (refused+returned combined); In-curs 3,234; Anulata 8,682; Netrimisa 10,401; Lipsa-awb 2,174 — Refuzata/Netrimisa/Lipsa each map to AWB equivalents or correctly-excluded states.
- Fix @ n/a (no fix): No change. The split-vs-merge presentation and the missing Lipsa-awb bucket are driven by data source (Frisbo aggregated_status vs courier-truth) and do not change the deliverability/expedition math.

## [LOW/DATA_SOURCE_DIFF] Returned-order loss = -transport: equivalent core logic to Scripturi  (profitability-aggregate)
- confidence: HIGH | needs_decision: False
- AWB: profitability.py line 342: returned profit_gross = -(shipping_cost); COGS zeroed (line 305); revenue tracked but excluded from net_revenue.
- Correct: Scripturi books transport on plecate (includes Refuzata) and excludes returned revenue from incasari — same economic outcome (a returned parcel costs its transport, earns nothing, product resold so COGS not lost). Equivalent. Note: this only works correctly once refused/unsuccessful_delivery are moved into 'returned' (finding 2).
- Evidence: profitability.py lines 305, 342; Scripturi api/profitability.py lines 2012-2016 (plecate includes Refuzata, In curs, Livrata) and 1992-1997 (incasari only Livrata).
- Fix @ N/A for core logic — depends on finding 2 being fixed so refused parcels actually reach this branch.: No change to the returned-loss formula itself; it is correct. Its benefit is currently denied to refused parcels because of the classification bug in finding 2.

## [LOW/DATA_SOURCE_DIFF] Returned-order handling (revenue/COGS excluded, only transport sunk) — equivalent to main P&L, no fix  (sku-profitability)
- confidence: HIGH | needs_decision: False
- AWB: For is_returned (lines 294-302) the SKU endpoint adds units_returned + sunk alloc_transport only; revenue and COGS are excluded. This matches main P&L profitability.py:341-342 (returned profit = -shipping) and PNL_KNOWLEDGE.md status table (returned: revenue tracked, COGS 0, shipping sunk).
- Correct: Same as AWB behavior — the returned-order treatment (exclude revenue+COGS, sink only outbound transport) is consistent with AWB's own spec and with the economic logic (product resold, courier leg paid). Scripturi likewise treats Refuzata as non-revenue. Core logic equivalent.
- Evidence: endpoint.py:294-302 vs profitability.py:341-342 and PNL_KNOWLEDGE.md:524-531. Confirmed equivalent; the only nuance is whether the RETURN leg transport should also be sunk, but main P&L does not do this either, so SKU report is consistent.
- Fix @ n/a (confirm equivalence): No fix required for the returned-order revenue/COGS exclusion; it is correct and matches the P&L. If the business later decides to also sink the return-leg courier cost, change BOTH P&L and SKU report together.

## [LOW/DATA_SOURCE_DIFF] Documented compute_final_outcome() bug (in_transit/out_for_delivery -> OTHER) is already fixed; delivered classification equivalent to Scripturi  (sales-velocity)
- confidence: HIGH | needs_decision: False
- AWB: computations.py current sets: IN_TRANSIT_WORKFLOW = {fulfilled, in_transit, out_for_delivery}; IN_TRANSIT_SHIPMENT = {in_transit, out_for_delivery, customer_pickup}; DELIVERY_PROBLEM_SHIPMENT includes deferred_delivery; NOT_SHIPPED_SHIPMENT includes generated_awb; NOT_SHIPPED_FULFILLMENT includes on_hold, ready_for_picking (lines 23-27). This matches the spec's 'Corrected' table, not the 'Current (buggy)' one.
- Correct: Spec deliverability_calculation_reference.md section 5 'Corrected' table requires exactly these additions. Velocity only consumes DELIVERED_OUTCOMES={DELIVERED} (endpoint.py line 41) and CANCELLED_OUTCOMES={CANCELLED}; DELIVERED_WORKFLOW={delivered, customer_pickup, in_parcel_locker} is unchanged and equivalent to Scripturi's Livrata classification of courier truth.
- Evidence: computations.py lines 14, 23-27 contain the corrected sets; the spec's buggy table omitted in_transit/out_for_delivery from IN_TRANSIT. The fix does not affect velocity's delivered/cancelled gates. Scripturi maps courier status -> Livrata (213408) for delivered; AWB maps Frisbo aggregated_status -> DELIVERED. Different sources, equivalent core meaning.
- Fix @ n/a: No fix. Confirmed the documented OTHER bug is resolved in current code and is irrelevant to velocity's delivered/cancelled gating; classification is equivalent to Scripturi modulo data source.

## [LOW/DATA_SOURCE_DIFF] Status bucket mapping is consistent with the store-level report and the spec; the documented OTHER bug does NOT affect this report  (product-deliverability)
- confidence: HIGH | needs_decision: False
- AWB: _get_status_bucket (lines 45-76) maps aggregated_status into delivered/returned/refused/in_transit/out_for_delivery/processing/cancelled/other.
- Correct: Verified identical, field-by-field, to deliverability.py's SQL CASE (lines 52-61) and the spec section 3 table. Crucially in_transit and out_for_delivery are correctly placed in IN_TRANSIT/OUT_FOR_DELIVERY sets, so unlike compute_final_outcome() (the spec's known-buggy function) this report does NOT mis-route them to OTHER. No fix needed.
- Evidence: product_deliverability.py:45-50 frozensets vs deliverability.py:52-61 SQL CASE vs spec lines 124-136: DELIVERED/RETURNED/REFUSED/IN_TRANSIT/OUT_FOR_DELIVERY/PROCESSING sets all match. Taxonomy difference vs Scripturi (separate returned+refused vs Scripturi folding returns into Refuzata) is a legitimate Frisbo-aggregated-status vs courier-truth data-source difference; delivered/left-warehouse concept is equivalent.
- Fix @ N/A: No change. Confirmed equivalence and that the documented compute_final_outcome OTHER bug is not present here.

## [LOW/DATA_SOURCE_DIFF] SKU cost exclusion (exclude_from_stock) is functionally equivalent to Scripturi's SKU exclusion rules for COGS  (cross-cutting)
- confidence: MEDIUM | needs_decision: False
- AWB: AWB drops SKUs flagged Product.exclude_from_stock=True from the COGS lookup map before the per-order loop (profitability.py:130-138), so those SKUs (mystery boxes / bundles, models/product.py:56-57) contribute no COGS. This is a product-level flag toggled in the Products UI.
- Correct: Equivalent core intent to Scripturi, which supports rule_type='sku' exclusion rules that remove whole ORDERS containing those SKUs from the report (profitability.py:1834,1841-1846). The semantics differ (AWB zeroes only the SKU's COGS but keeps the order/its revenue; Scripturi removes the entire order), but for the stated purpose — keeping non-stock bundle SKUs from polluting COGS — AWB's approach is reasonable and arguably better for revenue accuracy. No fix required; note the semantic difference for the user.
- Evidence: AWB: profitability.py:130-138 (`Product.exclude_from_stock == True` → pop from sku_costs_map, logs excluded count). Scripturi: profitability.py:1834 (excl_skus), 1841-1846 (order removed if any excluded SKU present). Different unit of exclusion (SKU-COGS vs whole-order) but both target non-stock/test SKUs.
- Fix @ N/A (equivalent; optional: expose order-level SKU exclusion if the user wants Scripturi parity): No fix. If the user wants exact Scripturi parity (drop the whole order for certain SKUs, e.g. pure test-bundle SKUs), add an optional SKU-based order exclusion alongside the test-tag filter.

## [LOW/NEEDS_DECISION] redirected / deferred_delivery / on_hold labelled in_transit in P&L+deliverability but DELIVERY_PROBLEM in SKU-risk  (status-classification)
- confidence: HIGH | needs_decision: True
- AWB: For aggregated_status in {redirected, deferred_delivery, on_hold}: deliverability.py (line 55) and profitability.py (line 299) classify them as in_transit, while compute_final_outcome() returns DELIVERY_PROBLEM (computations.py line 21). The same physical order therefore shows as 'În Tranzit' on the deliverability/P&L tabs and 'DELIVERY_PROBLEM' on the SKU-risk tab.
- Correct: Financially the three mappings AGREE (all treat these as shipped/still-active: not delivered, not returned, not cancelled), so no P&L number is wrong. DELIVERY_PROBLEM is a deliberately finer-grained SKU-risk label gated behind the include_delivery_problems flag (sku_risk/endpoint.py:310). Whether to surface them as a distinct 'problem/delivery-issue' bucket on the deliverability tab too, or keep them folded into În Tranzit, is a presentation/business-judgment call — not a correctness bug.
- Evidence: deliverability.py:55 in_transit list includes ['redirected','deferred_delivery','on_hold']; profitability.py:299 same; computations.py:21-22 DELIVERY_PROBLEM_WORKFLOW={'redirected','on_hold','deferred_delivery'}. Combined live volume per deliverability_calculation_reference.md §2a is tiny (redirected 65 + deferred_delivery 64 + on_hold 9 = 138).
- Fix @ C:/Users/Admin/Desktop/AWB Print/awb-print-manager/backend/app/api/sku_risk/computations.py lines 21-22 (or deliverability.py:55) — only if user wants a unified label: No code change required for correctness. If the user wants label consistency, fold DELIVERY_PROBLEM into IN_TRANSIT in compute_final_outcome (drop the DELIVERY_PROBLEM category), or add a matching 'delivery_problem' sub-bucket to the deliverability/P&L tabs. Document the chosen semantics either way.

## [LOW/NEEDS_DECISION] Packaging cost computed but silently dropped from the P&L  (profitability-aggregate)
- confidence: HIGH | needs_decision: True
- AWB: packaging_cost = config.packaging_cost_per_order (line 318, default 3.7 RON) is summed into stats and per_store but the P&L output forces packaging to {cu_tva:0, fara_tva:0} (lines 649, 785) and excludes it from total_operational (line 472, comment 'removed Packaging from sum').
- Correct: Either remove the dead computation or actually subtract packaging in the operational total. As-is, real packaging cost is excluded, overstating operating/net profit by 3.7 RON x delivered_count. PNL_KNOWLEDGE.md lines 575-576 acknowledge it is intentionally not displayed.
- Evidence: profitability.py line 472 (packaging removed from total_operational), lines 649 & 785 hardcode packaging to zero in output, while line 318 still computes it. Scripturi has no packaging line.
- Fix @ C:/Users/Admin/Desktop/AWB Print/awb-print-manager/backend/app/api/analytics/profitability.py lines 318, 472, 649, 785.: Decide: if packaging is a real cost, include d['packaging'] in total_operational_val (line 473) and emit tva_split(d['packaging']) instead of zeros; otherwise delete the packaging computation to avoid confusion. Business decision on whether packaging is already captured inside CSV transport or business_costs.

## [LOW/NEEDS_DECISION] Per-order VAT basis (per-order date) vs aggregate single-rate basis can diverge across the Aug-2025 cutoff  (profitability-perorder-multiperiod)
- confidence: MEDIUM | needs_decision: True
- AWB: profitability_orders.py computes dynamic_vat_rate PER ORDER from order_date vs the 2025-08-01 cutoff (lines 228-229), so a report spanning the cutoff applies 19% to pre-Aug orders and 21% to post-Aug orders within the same list.
- Correct: The aggregate engine uses a SINGLE dynamic_vat_rate for the whole period, derived from ref_date = date_to (profitability.py:66-78). For a report period that straddles 2025-08-01, the per-order sum of profit_net will NOT equal the aggregate net (different effective VAT). The per-order date-based approach is arguably MORE correct, but it makes the two endpoints irreconcilable across the boundary. This is a business-judgment call: either (a) make the aggregate also VAT-split per order date, or (b) make per-order use the single period rate.
- Evidence: profitability_orders.py:226-234 (per-order cutoff). Aggregate profitability.py:63-78 (single ref_date=date_to rate) and :447-451 (tva_split uses that one rate). Monthly reports never straddle the cutoff, so impact is confined to cross-cutoff custom ranges; today (2026-06) all live periods are post-cutoff so both yield 21% and agree.
- Fix @ C:/Users/Admin/Desktop/AWB Print/awb-print-manager/backend/app/api/analytics/profitability_orders.py:226-234 and profitability.py:63-78: Decide one basis and apply it in both. Recommended: adopt per-order-date VAT everywhere (more correct) and make the aggregate accumulate fara_tva per order using each order's date rather than a single period rate; or, if simplicity is preferred, force per-order to use the single period rate. Either way, align the two so per-order sums reconcile with the aggregate.

## [LOW/NEEDS_DECISION] Multi-period division map may silently drop stores into OTHER (coverage gap, e.g. Nubra)  (profitability-perorder-multiperiod)
- confidence: LOW | needs_decision: True
- AWB: DIVISION_MAP (multi_period_pnl.py:25-33) lists patterns for esteban/georgetalent, rossinails/belasil/gento/apreciat/nocturnalux/nocturna.ro, grandia, the TREND set, and bonhaus/nocturna.bg. A store whose name matches none (the Scripturi sister business includes a 'Nubra'/NUB store, and 'nubra' is not a pattern) maps to 'OTHER' (line 46) and is excluded from every named subsidiary, while still contributing to the consolidated total taken from the aggregate pnl. This makes sum(subsidiaries) != consolidated whenever an unmapped store has activity.
- Correct: Either every active AWB store name must match exactly one division pattern, or the UI/endpoint must surface an explicit 'OTHER' division so subsidiaries reconcile to consolidated. This requires confirming AWB's actual store list (cannot query AWB prod DB); Scripturi shows a NUB/Nubra store exists in the shared business.
- Evidence: multi_period_pnl.py:25-46 (DIVISION_MAP + _store_to_division fallback to OTHER). Scripturi DB distinct prefixes include NUB (bmuwvv-jy.myshopify.com) with no matching division pattern in AWB's map.
- Fix @ C:/Users/Admin/Desktop/AWB Print/awb-print-manager/backend/app/api/analytics/multi_period_pnl.py:25-46: Confirm whether Nubra (and any other store) exists in AWB. If yes, add it to the correct division pattern list, or add an explicit 'OTHER' bucket to subsidiary_names so the per-subsidiary rows sum back to consolidated. User must confirm the canonical store->division assignment.