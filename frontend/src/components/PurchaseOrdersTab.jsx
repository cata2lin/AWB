/**
 * PurchaseOrdersTab — Purchase Order management (single panel, no sub-tabs).
 */
import POManagerPanel from './POManagerPanel'

export default function PurchaseOrdersTab() {
    return <POManagerPanel />
}
